"use strict";

var assert = require('assert');

describe('test', function() {
    describe('#test()', function() {
        it('should be fine', function() {
            assert.equal(true, true);
        });
    });
});